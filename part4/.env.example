# Copy to part4/.env and fill in local values. Never commit part4/.env.
PGUSER=postgres
PGPASSWORD=change-me
PGHOST=localhost
PGPORT=5432
PGDATABASE=part3

# Optional overrides
# PART4_DB_URL=postgresql+psycopg://user:pass@host:5432/part3
# PART4_PORT=5055
# PART4_POLL_SECONDS=60
